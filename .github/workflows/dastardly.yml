name: Dastardly Action
on:
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:

jobs:
  dastardly:
    runs-on: ubuntu-latest
    steps:
    - name: Run Dastardly Action Step
      uses: PortSwigger/dastardly-github-action@main
      with:
        target-url: 'http://localhost:3000'
